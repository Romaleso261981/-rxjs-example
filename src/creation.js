import {Observable} from 'rxjs'
/*
import { from } from 'rxjs/src';
const stream$ = of(1,2,3,6,5,4,7)
stream$.subscribe(value => {
  console.log(value);
})

const arr& = from([1,2,3,6,5,4,8,8]).pipe(
  scan((acc, v)=>acc.concat(v), [])
)
*/
